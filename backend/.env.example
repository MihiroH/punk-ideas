NODE_ENV=local

# frontendのベースURL
APP_BASE_URL=http://localhost:8000

API_CORS_ALLOW_ORIGIN=*
API_PORT=8000

DB_CONNECTION=pgsql
DB_HOST=localhost
DB_PORT=5432
DB_DATABASE=punk-idea-local
DB_USERNAME=punk-idea-user
DB_PASSWORD=password

MAILPIT_SMTP_HOST=localhost
MAILPIT_SMTP_PORT=1025
MAILPIT_WEBUI_PORT=8025

MAIL_REGISTRATION_VERIFICATION_URL=${APP_BASE_URL}/auth/verify-email
MAIL_CHANGE_EMAIL_VERIFICATION_URL=${APP_BASE_URL}/auth/verify-email-change
MAIL_FROM_ADDRESS=no-reply@example.com

# This was inserted by `prisma init`:
# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# DATABASE_URL="postgresql://johndoe:randompassword@localhost:5432/mydb?schema=public"
DATABASE_URL=postgresql://${DB_USERNAME}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_DATABASE}?schema=public

JWT_SECRET=b4f37583e8f0002b812585491618ca8c827662c04c026c5bfecc43ee565376b9
JWT_EXPIRES_IN=1h

MINIO_ROOT_USER=miniouser
MINIO_ROOT_PASSWORD=password
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001

AWS_REGION=ap-northeast-1
AWS_ACCESS_KEY_ID=miniouser
AWS_SECRET_ACCESS_KEY=password
S3_BUCKET=local-bucket
S3_ENDPOINT=http://localhost:${MINIO_PORT} # NODE_ENV=localのときだけ設定する想定
